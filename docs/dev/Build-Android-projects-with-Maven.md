# Build Android projects with Maven

Dev env requirements:
1. Follow http://code.google.com/p/maven-android-plugin/wiki/GettingStarted
1. Install platforms:
```bash
android update sdk --no-ui --obsolete --force
```
1. Install 32-bit libs if you use 64-bit OS:
```bash
sudo apt-get install ia32-libs
```
