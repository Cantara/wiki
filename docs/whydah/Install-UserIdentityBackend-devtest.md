# Install UserIdentityBackend - devtest

- Docker Data volume container
  - useridentitybackend\_override.properties
  - prodInitData (csv fles for import), can possibly skip this
  - logback.xml, can possibly skip this
- UIB in Docker
  - Embedded ApacheDS
  - Embedded HSQLDB
  - lucene data

#### Install from source
